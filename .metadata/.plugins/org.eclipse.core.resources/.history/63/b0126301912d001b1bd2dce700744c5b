package com;

import java.util.*;

public class MatrixClass {
	public static void main(String args[]) {
		Scanner obj = new Scanner(System.in);
		int bigRow = 3, bigColumn = 3;
		int smallRow = 2, smallColumn = 2;
		int count = 0;
		int k = 0, l = 0 , i = 0;
		int flag = 0;
		int tempI = 0, tempJ = 0;

		int[][] biggerMat = new int[bigRow][bigColumn];
		int[][] smallerMat = new int[smallRow][smallColumn];

		for (i = 0; i < bigRow; i++) {
			for (int j = 0; j < bigColumn; j++) {
				biggerMat[i][j] = obj.nextInt();
			}
		}

		for (i = 0; i < smallRow; i++) {
			for (int j = 0; j < smallColumn; j++) {
				smallerMat[i][j] = obj.nextInt();
			}
		}

		for (int j = 0; j < bigColumn; j++) {
			i = tempI;
			j = tempJ;
			count = 0;
			flag = 0;
			while (smallerMat[k][l] == biggerMat[tempI][tempJ]) {

				flag = 1;
				count++;

				if (l < smallRow - 1) {
					tempJ++;
					l++;
				}
				if (l == smallRow) {
					l = 0;
					tempI++;
					tempJ = 0;
					k++;
				}
			}
			if(count == smallRow * smallRow) {
				break;
			}
			i++;
		}

		if (flag == 1)
			System.out.print("TRUE");
		else
			System.out.print("FALSE");
	}

}
