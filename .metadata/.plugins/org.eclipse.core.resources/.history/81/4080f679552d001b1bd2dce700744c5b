package com;

public class SquareClass {
	public static void main(String args[]) {
		int start = 20, stop =100;
		
		int startSqrt = (int)Math.sqrt(start);
		int stopSqrt = (int)Math.sqrt(stop);
		
//		System.out.print(startSqrt + " " + stopSqrt);
		
		for(int i = startSqrt + 1; i <= stopSqrt ; i++) {
			System.out.println(i * i);
		}
	}
}
