package com;

import java.util.*;

public class CacheClass {
	public static void main(String args[]) {
		int num = 2;
		int numQuery = 7;
		int jPointer = 0;
		String queries = "SET 1 2 SET 2 3 SET 1 5 SET 4 5 SET 6 7 GET 4 GET 1";
		String[] strArr = queries.split(" ");
		
		TreeMap<Integer, Integer> setMap = new TreeMap<Integer,Integer>();  
		
		for(int i = 0 ; i < strArr.length ; i++) {
			if(strArr[i].equals("SET")) {
				if(jPointer == num) {
					setMap.remove(setMap.firstKey());
					jPointer = 0;
					setMap.put(Integer.parseInt(strArr[i+1]), Integer.parseInt(strArr[i+2]));
					jPointer++;
				}
				else {
					setMap.put(Integer.parseInt(strArr[i+1]), Integer.parseInt(strArr[i+2]));
					jPointer++;
				}
			}
//			else if(strArr[i].equals("GET")) {
//				
//			}
		}
		for(Map.Entry m:setMap.entrySet()){  
			   System.out.println(m.getKey()+" "+m.getValue());  
		}  
	}
}
