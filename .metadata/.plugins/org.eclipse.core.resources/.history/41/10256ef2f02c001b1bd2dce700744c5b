package com;

public class Main {

	public static void main(String[] args) {
		String str = "Program";
		int strLength = str.length();
		int midPoint = strLength / 2;
		String resultant = "";
		int jPointer = midPoint;

		while (jPointer != str.length()) {
			resultant = resultant + str.charAt(jPointer);
			System.out.println(resultant);
			jPointer++;
		}

		if (jPointer == str.length()) {
			jPointer = 0;
			while (jPointer != midPoint) {
				resultant = resultant + str.charAt(jPointer);
				System.out.println(resultant);
				jPointer++;
			}
		}

	}

}

/* PROGRAM */

/*
 ************** G GR1 GRA11 GRAM1111 GRAMP11111 GRAMPR111111 GRAMPRO11111111
 * 
 * 
 * 
 * 
 */